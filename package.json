{
  "scripts": {
    "docker:start": "docker-compose up --abort-on-container-exit",
    "docker:stop": "docker-compose down",
    "docker:create-persistent": "docker-compose -f docker-compose-persistent.yml up -d"
  }
}
