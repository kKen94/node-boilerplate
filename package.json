{
  "scripts": {
    "docker:start": "docker-compose up --abort-on-container-exit --force-recreate",
    "docker:stop": "docker-compose down --rmi local",
    "docker:create-persistent": "docker-compose -f docker-compose-persistent.yml up"
  }
}
