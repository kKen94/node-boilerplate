_format_version: '1.1'

services:
- name: user.api
  host: user.api
  path: /
  connect_timeout: 60000
  read_timeout: 60000
  retries: 5
  write_timeout: 60000
  routes:
  - name: user.api
    methods:
    - GET
    - POST
    - PUT
    - DELETE
    paths:
    - /user
