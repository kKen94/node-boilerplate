version: "3.7"
services:

  user-data:
    image: postgres:11.5
    networks:
      - t2c-persistent
    container_name: user-data
    ports:
      - "54321:5432"
    restart: always
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=user

networks:
  t2c-persistent:
